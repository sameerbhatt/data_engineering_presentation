import React from 'react'
import ReactDOM from 'react-dom/client'
import DataEngineeringPresentation from '../data_engg_presentation'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <DataEngineeringPresentation />
  </React.StrictMode>,
)
